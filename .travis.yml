---

dist: xenial

language: python

python: 3.6

install:
  - pip install ansible

before_script:
  - echo "core.trace-share ansible_host=localhost" > inventory

script:
  # Check the role/playbook's syntax
  - ansible-playbook -i inventory provisioning/core.yml --syntax-check

  # Run the role/playbook with ansible-playbook
  - ansible-playbook -i inventory provisioning/core.yml --connection=local
