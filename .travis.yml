---

dist: xenial

language: python

python: 3.6

install:
  - pip install ansible

before_script:
  - echo "localhost" > inventory

script:
  - ansible-playbook -i inventory provisioning/ci-test.yml --syntax-check 
